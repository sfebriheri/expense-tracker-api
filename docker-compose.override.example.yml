# Optional local-dev convenience overrides.
# Copy to docker-compose.override.yml to use (it is picked up automatically by `docker compose`).
services:
  api:
    build:
      target: build
    command: npm run dev
    volumes:
      - ./src:/app/src
    environment:
      NODE_ENV: development
